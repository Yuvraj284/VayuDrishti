/**
 * AtmosphericParticles — removed.
 * The 2D canvas cyclone handles all visual atmosphere.
 * This stub is kept to avoid import errors if referenced elsewhere.
 */
export default function AtmosphericParticles() {
  return null
}
